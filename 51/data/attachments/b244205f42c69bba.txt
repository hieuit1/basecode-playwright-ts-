══════════════════════════════════════
   🟡 KẾT QUẢ CHẤM ĐIỂM SEO
══════════════════════════════════════
   Điểm số:     71/100
   Đánh giá:    KHÁ
   Ngưỡng đạt:  96%
   Kết quả:     FAIL
──────────────────────────────────────
   Tổng tiêu chí:  24
   ✅ Đạt:          17
   ❌ Không đạt:    7
══════════════════════════════════════

📋 CHI TIẾT LỖI CẦN KHẮC PHỤC (7/24):
--- HEADING STRUCTURE ---
   1. [Heading phân cấp hợp lệ (3 lỗi)]
      → Heading phân cấp sai: Nhảy cấp từ H2 → H4 ("Giữ trọn hồn ẩm thực Nam Bộ"); Nhảy cấp từ H2 → H4 ("Không gian"); Nhảy cấp từ H2 → H6 ("Giỏ hàng của bạn")
--- PERFORMANCE ---
   2. [[📱 MOBILE (ƯU TIÊN)] Tổng điểm Performance: 52/100 (≥ 60)]
      → [📱 MOBILE (ƯU TIÊN)] Điểm Performance 52/100 dưới ngưỡng 60. Phân tích chi tiết LCP/CLS/INP bên dưới...
   3. [[📱 MOBILE (ƯU TIÊN)] LCP (Largest Contentful Paint): 4506ms (< 2500ms)]
      → [📱 MOBILE (ƯU TIÊN)] LCP quá cao: 4506ms (chuẩn: < 2.5s)
      → Thủ phạm LCP: <img class="w-100" src="upload/photo/banner-ve-chung-toi-3017.png" alt="LÀNG NƯỚNG NAM BỘ - DƯƠNG ĐỨC HIỀN">

[Vấn đề]: Phần tử LCP là ảnh nhưng không có thuộc tính width/height trong HTML.
[Thông số hiện tại]: Kích thước không xác định từ HTML thô.
[Chỉ thị hành động]: Dùng DevTools (Computed tab) đo kích thước hiển thị thực tế. Resize ảnh gốc bằng 2x kích thước hiển thị (định dạng .webp, quality 75-80), khai báo kích thước cứng vào HTML và thêm thẻ preload.
[Code thay thế]:
<!-- Đặt thẻ preload trong <head> -->
<link rel="preload" as="image" href="[URL_ẢNH]" fetchpriority="high">

<!-- Sửa thẻ <img> (thay X, Y bằng số đo thực tế): -->
<img src="[URL_ẢNH]" width="X" height="Y" style="aspect-ratio: X / Y; width: 100%; height: auto; object-fit: cover;" alt="LCP Image">
   4. [[📱 MOBILE (ƯU TIÊN)] CLS (Cumulative Layout Shift): 0.287 (< 0.1)]
      → [📱 MOBILE (ƯU TIÊN)] CLS quá cao: 0.287 (chuẩn: < 0.1)
      → Thủ phạm CLS:
          - <section class="wrap-about py-3 py-lg-4">
          - <div class="col-12 col-lg-6 ">
          - <div class="wrap-home">
          - <section class="wrap-tamnhin py-3 py-lg-4">

[Vấn đề]: Các phần tử (không phải ảnh có sẵn width/height rõ ràng) thay đổi kích thước đột ngột trong lúc tải trang.
[Thông số hiện tại]: Phần tử gây nhảy: <section class="wrap-about py-3 py-lg-4"> | <div class="col-12 col-lg-6 "> | <div class="wrap-home">.
[Chỉ thị hành động]: Nếu là font chữ, dùng size-adjust. Nếu là ảnh/iframe, bắt buộc khai báo width/height hoặc CSS aspect-ratio. Đối với ảnh lazy-load, phải có kích thước giữ chỗ.
[Code thay thế]:
/* Giải pháp 1: Giữ chỗ bằng aspect-ratio */
.cls-element {
  aspect-ratio: 4 / 3; /* Điền tỷ lệ đo được */
  width: 100%;
}

/* Giải pháp 2: Sửa nhảy font bằng size-adjust */
@font-face {
  font-family: 'Fallback Font';
  src: local('Arial');
  size-adjust: 90%; /* Điều chỉnh để khớp font chính */
}
   5. [[💻 Desktop] Tổng điểm Performance: 48/100 (≥ 60)]
      → [💻 Desktop] Điểm Performance 48/100 dưới ngưỡng 60. Phân tích chi tiết LCP/CLS/INP bên dưới...
   6. [[💻 Desktop] LCP (Largest Contentful Paint): 5784ms (< 2500ms)]
      → [💻 Desktop] LCP quá cao: 5784ms (chuẩn: < 2.5s)
      → Thủ phạm LCP: <img class="w-100" src="upload/photo/banner-ve-chung-toi-3017.png" alt="LÀNG NƯỚNG NAM BỘ - DƯƠNG ĐỨC HIỀN">

[Vấn đề]: Phần tử LCP là ảnh nhưng không có thuộc tính width/height trong HTML.
[Thông số hiện tại]: Kích thước không xác định từ HTML thô.
[Chỉ thị hành động]: Dùng DevTools (Computed tab) đo kích thước hiển thị thực tế. Resize ảnh gốc bằng 2x kích thước hiển thị (định dạng .webp, quality 75-80), khai báo kích thước cứng vào HTML và thêm thẻ preload.
[Code thay thế]:
<!-- Đặt thẻ preload trong <head> -->
<link rel="preload" as="image" href="[URL_ẢNH]" fetchpriority="high">

<!-- Sửa thẻ <img> (thay X, Y bằng số đo thực tế): -->
<img src="[URL_ẢNH]" width="X" height="Y" style="aspect-ratio: X / Y; width: 100%; height: auto; object-fit: cover;" alt="LCP Image">
   7. [[💻 Desktop] CLS (Cumulative Layout Shift): 0.398 (< 0.1)]
      → [💻 Desktop] CLS quá cao: 0.398 (chuẩn: < 0.1)
      → Thủ phạm CLS:
          - <div class="wrap-home">
          - <section class="wrap-tamnhin py-3 py-lg-4">
          - <section class="wrap-tamnhin py-3 py-lg-4">

[Vấn đề]: Các phần tử (không phải ảnh có sẵn width/height rõ ràng) thay đổi kích thước đột ngột trong lúc tải trang.
[Thông số hiện tại]: Phần tử gây nhảy: <div class="wrap-home"> | <section class="wrap-tamnhin py-3 py-lg-4"> | <section class="wrap-tamnhin py-3 py-lg-4">.
[Chỉ thị hành động]: Nếu là font chữ, dùng size-adjust. Nếu là ảnh/iframe, bắt buộc khai báo width/height hoặc CSS aspect-ratio. Đối với ảnh lazy-load, phải có kích thước giữ chỗ.
[Code thay thế]:
/* Giải pháp 1: Giữ chỗ bằng aspect-ratio */
.cls-element {
  aspect-ratio: 4 / 3; /* Điền tỷ lệ đo được */
  width: 100%;
}

/* Giải pháp 2: Sửa nhảy font bằng size-adjust */
@font-face {
  font-family: 'Fallback Font';
  src: local('Arial');
  size-adjust: 90%; /* Điều chỉnh để khớp font chính */
}